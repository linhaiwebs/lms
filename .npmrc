# pnpm 配置 — 解决 Create React App (react-scripts 4.x) 的依赖兼容问题
#
# 问题：pnpm 严格隔离 node_modules，导致 babel-preset-react-app 找到的
#       @babel/core 版本与 react-scripts 内置版本不一致
# 解决：shamefully-hoist 将所有包提升到根 node_modules，模拟 npm 的扁平结构

shamefully-hoist=true
